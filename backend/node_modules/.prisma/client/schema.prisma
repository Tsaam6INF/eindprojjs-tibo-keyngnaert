generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Video {
  id        Int      @id @default(autoincrement())
  title     String
  filename  String
  thumbnail String
  createdAt DateTime @default(now())
}
